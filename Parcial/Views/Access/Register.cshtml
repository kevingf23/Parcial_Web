<!--recibimos el model -->
@model Parcial.Models.Usuario
@{
    ViewBag.Title = "Agregar Usuario";
}

<h2>@ViewBag.Title</h2>

<div class="row">

    @using (Html.BeginForm("Add", "User", FormMethod.Post, new { role = "form", @class = "" }))
    {
        //SON HELPER
        @Html.AntiForgeryToken() //PETICIONES DESDE AFUERA SEGURIDAD
        @Html.ValidationMessage("error", new { @class = "text-danger" })// se muestra cuando hay error clase bootstrap

        @Html.LabelFor(u => u.User)
        @Html.TextBoxFor(u => u.User, new { @class = "form-control" })
        @Html.ValidationMessage("Email", new { @class = "text-danger" })

        <br>
        @Html.LabelFor(u => u.Password)
        @Html.PasswordFor(u => u.Password, new { @class = "form-control" })

        @Html.ValidationMessage("Password", new { @class = "text-danger" })

        @*@Html.LabelFor(d => d.ConfirmPassword)
        @Html.PasswordFor(d => d.ConfirmPassword, new { @class = "form-control" })

        @Html.LabelFor(d => d.Edad)
        @Html.TextBoxFor(d => d.Edad, new { @class = "form-control" })
        @Html.ValidationMessage("Edad", new { @class = "text-danger" })*@

        <div style="text-align:right">
            <input type="submit" class="btn btn-success" value="Guardar" />
        </div>
    }

    <div class="col-md-12">

    </div>
</div>
